jest.mock('aes-js');
const aesjs = require('aes-js');
const AES = require('../../../utils/crypto/oldAES');

describe('AES encrypt', () => {
    it('should encrypt byte', () => {
        expect(true).toBe(true);
    });
});
