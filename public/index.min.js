const filesFormGroup=document.querySelector(".files_form_group"),fileInput=filesFormGroup.querySelector('input[type="file"]'),textInput=filesFormGroup.querySelector("textarea");fileInput.addEventListener("change",e=>{const t=[];for(const o of e.target.files)t.push(o.name);textInput.value=t.join("\n"),textInput.title=t.join("\n")});